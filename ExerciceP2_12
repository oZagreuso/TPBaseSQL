SELECT emp.DEPTNO, ENAME, DNAME, HIREDATE FROM emp, dept
WHERE emp.DEPTNO = dept.DEPTNO and DNAME = 'RESEARCH' AND HIREDATE IN
(SELECT  HIREDATE FROM emp, dept WHERE emp.DEPTNO = dept.DEPTNO and DNAME ='SALES');